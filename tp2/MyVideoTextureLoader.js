import * as THREE from "three";

export class MyVideoTextureLoader {

    /**
     * @param {string} path
     * @param {((data: THREE.VideoTexture) => void)=} onLoad
     */
    load(path, onLoad) {
        const videoElement = document.createElement("video");
    }
}